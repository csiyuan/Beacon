import AboutClient from './AboutClient';

export const metadata = {
  title: 'About - Beacon Media Solutions',
  description:
    'The bridge between great creatives and the brands that need them. Built in Singapore, scaling across Southeast Asia.',
};

export default function AboutPage() {
  return <AboutClient />;
}
